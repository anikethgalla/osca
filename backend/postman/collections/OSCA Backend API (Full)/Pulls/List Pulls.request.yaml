$kind: http-request
name: List Pulls
url: "{{BASE_URL}}/api/v1/pulls/{{owner}}/{{repo}}?page=1&limit=10"
method: GET
headers:
  Authorization: Bearer {{JWT_TOKEN}}
queryParams:
  page: "1"
  limit: "10"
order: 1000
