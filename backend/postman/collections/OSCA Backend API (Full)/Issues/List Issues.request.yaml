$kind: http-request
name: List Issues
url: "{{BASE_URL}}/api/v1/issues/{{owner}}/{{repo}}?page=2&limit=10"
method: GET
headers:
  Authorization: Bearer {{JWT_TOKEN}}
queryParams:
  page: "2"
  limit: "10"
order: 1000
