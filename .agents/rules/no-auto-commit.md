## no-auto-commit

Rules:
- NEVER run `git commit` automatically without first explicitly asking the user for their permission and confirming the commit message with them.
- Always stage files and show the user what will be committed, but wait for their 'yes' before executing the commit.
